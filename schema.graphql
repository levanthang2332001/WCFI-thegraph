
type Token @entity {
  id: ID!
  tokenId: BigInt!
  owner: Bytes # address
  sender: Bytes # address
  price: BigInt
  country: BigInt
}

type TotalMint @entity {
  id: ID!
  address: Bytes!
  total: BigInt
}
